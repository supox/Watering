<% provide(:title, t(:new_plan)) %>
<h1><%= t(:new_plan)%></h1>
<div class="row">
	<div class="span6 offset3">
		<%= semantic_form_for(@plan, url: url_for(action: 'create')) do |f| %>
			<%= render 'fields', f: f %>
		<% end %>
	</div>
</div>
